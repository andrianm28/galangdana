{
  "name": "galangdana",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "bun run --cwd apps/web dev",
    "dev:api": "bun run --cwd apps/api dev",
    "build": "bun run --cwd apps/web build",
    "test": "bun test packages/money packages/payments packages/contracts packages/db packages/media packages/search apps/api",
    "test:ui": "bun run --cwd packages/ui test",
    "test:web": "bun run --cwd apps/web test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "bun run --filter='*' typecheck",
    "db:generate": "bun run --cwd packages/db drizzle-kit generate",
    "db:migrate": "bun run --cwd packages/db db:migrate",
    "db:seed": "bun run --cwd packages/db db:seed"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.4.0",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "@sinclair/typebox": "0.33.24"
  }
}
