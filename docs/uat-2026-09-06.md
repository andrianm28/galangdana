# UAT End-to-End 2026-09-06 — FundForIndonesia

Metode: browser Chromium headless (Playwright) mengemudikan stack dev terisolasi
(API :3101 + web :5193, DB/Redis/MinIO/Meili bersama, Rekening sandbox Sumopod).
Semua pembayaran bernilai non-nyata (sandbox / mock). Prod tidak disentuh kecuali
webhook sandbox yang memang dikonfigurasi ke URL prod (terverifikasi ditolak
fail-closed). Cleanup penuh setelahnya kecuali dinyatakan lain.

## Hasil per journey

### J1 — Consumer ✅ LULUS PENUH
- Home (8 kartu), search "banjir" (1 hit), detail + CTA Donasi
- Preset Rp50rb → payment-option **menawarkan QRIS** → contribute →
  redirect nyata ke `pay-sandbox.sumopod.com` (tidak dibayar; link kedaluwarsa 24 jam)
- Jalur VA: status pending + nomor VA → settle via mock webhook (`processed`) →
  status `paid`
- Kuitansi: render + `terbilang` "lima puluh ribu" ✅ · UUID palsu → 404 ✅

### J2 — Campaigner ✅ LULUS PENUH (dengan 4 temuan skrip, 0 bug produk)
- Login OTP via UI ✅ · draft track medis ✅ · tujuan/judul/target ✅
- Cerita guided 6 pertanyaan → `campaign_story_answers` (6 baris) ✅
- Pasien → `patients` (1 baris) ✅ · dokumen presign→PUT MinIO→confirm (1 objek) ✅
- OTP wizard ✅ · campaign terbuat ✅
- KYC lengkap (identitas/KTP-swafoto/hold/summary) → submit → `pending` ✅
- Dashboard "Menunggu Peninjauan" ✅ · DB: status `pending_review`, KYC `pending`
- Temuan UAT-positif: rate limiter OTP **menolak request ke-4 dengan benar**
  (skrip harus rotasi nomor + clear key prefix sendiri, meniru beforeEach test repo)

### J3 — Admin ✅ LULUS PENUH
- Promosi admin via DB (sesuai proses terdokumentasi: tanpa invite flow) ✅
- Antrean moderasi + detail menampilkan PII KYC (nama + NIK) ✅
- Request revision (cerita + catatan) → `needs_revision` ✅
- Campaigner revise (Simpan Cerita + Ajukan Ulang) → `pending_review` ✅
- Approve → `active` ✅
- Wizard pencairan (rekening BCA baru, bukti, partial Rp50.000, OTP, submit) → `requested` ✅
- Admin1 approve → `approved` ✅
- **Negatif four-eyes: admin1 klik Bayar → status tetap `approved`, `paid_by` NULL** ✅
- Admin2 Bayar → `paid`, `approved_by <> paid_by` ✅ (mock payout, tanpa uang nyata)
- Jejak dana publik `/pencairan-dana` menampilkan payout ✅

### J4 — Penyelesaian QRIS sandbox ⚠️ SEBAGIAN (blokir ekspektasi, bukan bug)
- Halaman bayar sandbox: tombol **"Click here to simulate your payment"** diklik →
  "Payment simulated, waiting for confirmation…"
- Webhook `payment.completed` **nyata terkirim ke URL prod** (terlihat di journal
  API prod 10:15:41 + retry 10:15:47) dan **tertolak fail-closed dengan benar**
  (`Sumopod is not configured: SUMOPOD_WEBHOOK_SECRET is not set`, tanpa efek DB)
- Donasi QRIS tetap `pending`: penyelesaian penuh (status paid via QRIS) butuh
  kredensial **produksi** (API key + secret + base URL prod) — sama dengan
  prasyarat cutover. Tidak ada bug: rantai sandbox→webhook→gate terbukti bekerja
  dengan trafik nyata.

## Temuan selama UAT (semua sudah ditangani)
1. `pkill` berpola terlalu luas sempat mematikan API prod (~2 mnt, 08:33–08:35 UTC);
   di-restart via systemd, terverifikasi 200. Aturan baru: proses dev hanya via PID
   eksplisit di port ≥3101.
2. Sweep cleanup `idempotency_keys` 6-jam menghapus 676 baris (milik test-suite lain
   yang jalan hari ini, bukan hanya UAT). Tidak merusak kode, tapi pelajaran:
   cleanup harus by-ID eksplisit. Tidak ada run konkuren yang sedang jalan saat itu.
3. Baris `test-ord` + user `+6281900000019` milik penulis lain — **tidak disentuh**.

## Cleanup — TERVERIFIKASI BERSIH
- DB: 3 campaign, 4 draft, 4 donasi, 1 disbursement, 4 user, 13 session, 16 OTP,
  24 story answer, 4 patient, 2 verifikasi, N revisi/dokumen/outbox/event → **0 residu**
  (cek by-ID eksplisit + pola `uat-%`/`evt-uat-*`/telepon UAT)
- MinIO `campaign-documents`: 7 objek (4 dokumen draft, KTP, selfie, proof) terhapus
- Dev stack dimatikan (port 3101/5193 bebas); prod api+web 200

## Sisa untuk produksi
Kredensial Sumopod produksi → ikuti runbook 5 langkah di `.env.example`, lalu
ulangi simulasi dashboard untuk melihat donasi QRIS pertama berstatus `paid`.
